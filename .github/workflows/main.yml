name: Python application

on: [push]

jobs:
  checks:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - name: Install
      uses: abatilo/actions-poetry@v1.5.0
      with:
        python_version: 3.7.5
        poetry_version: 1.0.0
        working_directory: ./punctuator # Optional, defaults to '.'
        args: install

    - name: Set up cache
      uses: actions/cache@v1
      with:
        path: ./punctuator/.venv
        key: venv-${{ runner.os }}-${{ steps.full-python-version.outputs.version }}-${{ hashFiles('**/poetry.lock') }}

    - name: Lint with flake8
      uses: abatilo/actions-poetry@v1.5.0
      with:
        python_version: 3.7.5
        poetry_version: 1.0.0
        working_directory: ./punctuator
        args: run flake8 punctuator --count --select=E9,F63,F7,F82 --show-source --statistics

    - name: Formatter
      uses: abatilo/actions-poetry@v1.5.0
      with:
        python_version: 3.7.5
        poetry_version: 1.0.0
        working_directory: ./punctuator
        args: run yapf -r -d --style pep8 punctuator

    - name: Import optimization
      uses: abatilo/actions-poetry@v1.5.0
      with:
        python_version: 3.7.5
        poetry_version: 1.0.0
        working_directory: ./punctuator
        args: run isort -rc -d -c punctuator

